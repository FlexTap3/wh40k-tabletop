/* ==== WP3D-13: extras ==== Procedural WebAudio (dice clatter, piece thunk, hover tick —
 * gesture-gated, persisted mute), 📸 battle-photo (toBlob + navigator.share, download
 * fallback), camera preset buttons (¾ / top / table-level). All DOM lives in an overlay
 * this pack creates inside #boardwrap; no core-HTML edits. */

/* createExtras(deps) -> { tick(dtMs, state), dispose() }
 * deps = { THREE, canvas, renderer, rig, scene, sceneSync, motion, bridge, container } */
export function createExtras(deps) {
  /* stub — P4 fills this file */
  return { tick(dtMs, state) {}, dispose() {} };
}
