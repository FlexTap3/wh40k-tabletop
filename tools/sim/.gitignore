# heavy per-action logs regenerate deterministically from the code + seed — don't track them.
# Battle reports (game-NN.md) and summaries (game-NN.summary.json) ARE deliverables — tracked.
out/*.jsonl
findings.jsonl
